/** Empty on purpose: `ToolPage` overwrites the options object from the grid, so a control
 * here would be discarded on run. That is exactly what the old blank-page button did. */
export default function OrganizeOptionsPanel() {
  return null
}
